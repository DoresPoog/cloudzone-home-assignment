import type { Config } from "@react-router/dev/config";

export default {
  // Setting ssr to false will disable server-side rendering, essentially making this app a SPA.
  ssr: false,
} satisfies Config;
